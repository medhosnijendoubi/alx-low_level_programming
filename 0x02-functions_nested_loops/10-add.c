#include "main.h"

/**
 * add - adds two integers and returns the result
 * @p: int to be added to k
 * @k: int to be added to p
 * Return: sum of p and k
 */
int add(int p, int k)
{
	int x;

	x = p + k;

	return (x);
}
